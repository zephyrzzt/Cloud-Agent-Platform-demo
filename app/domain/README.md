# Domain

Domain modules hold platform-neutral business objects.

Phase three adds `task.py`, which defines task requests, repository refs, execution limits, statuses, task records, and task results. These objects are shared by the scheduler, worker, orchestrator, and task store.

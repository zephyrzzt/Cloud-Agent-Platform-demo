# Reviewer Debug

Reviewer debug grants are controlled escalation records.

The trigger checks the failure ledger for repeated fingerprints. When the threshold is reached, the service can issue a short-lived grant for specific diagnostic tools. The grant is data, not an automatic policy bypass.
